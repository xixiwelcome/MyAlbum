<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activity.MainActivity">

    <LinearLayout
        android:id="@+id/ll_main_title_bar"
        android:layout_width="match_parent"
        android:gravity="center"
        android:layout_height="45dp"
        android:background="@drawable/bg_main_title_bar">

        <TextView
            android:id="@+id/tv_latest"
            style="@style/TitleBarTextViewStyle"
            android:text="@string/title_bar_latest" />

        <TextView
            android:id="@+id/tv_favorite"
            style="@style/TitleBarTextViewStyle"
            android:text="@string/title_bar_favorite" />

    </LinearLayout>

    <FrameLayout
        android:id="@+id/fl_list_latest"
        android:layout_below="@id/ll_main_title_bar"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <FrameLayout
        android:id="@+id/fl_list_favorite"
        android:layout_below="@id/ll_main_title_bar"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</RelativeLayout>
